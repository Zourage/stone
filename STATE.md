# STATE — read this first, every session

Current state of the language and the method, kept short on purpose. `decisions.md` is the append-only history with rationale; read the entries cited here when you need the why. When anything changes: append to `decisions.md`, then update this file and the spec it touches.

## Objectives (D125) — what work is for

Two, and they stand until the maintainer changes them. **Tag any proposed work to one of them before doing it.**

**Goal 1 — can an LLM translate this language?** The acceptance test, bar set in D50: cold agents given the train split as `English ||| script` and nothing else, **100% meaning-correct** stone → English and **≥90% exact** English → stone. **Passed three times** — 97.5% (test 5), then 91.2% and 89.6% on fresh draws, the last two with **zero real model misses**. **Currently unmeasured**: 129 sentences and 18 roots have landed since test 7, so those numbers describe a corpus that no longer exists (D104).

**Goal 2 — can a script translate this language?** `realize.py` and `compose.py`, deterministic, no model (D106–D108). On the corpus at 1424: stone → English **21.8% exact** (43.8% with near-matches, nothing unproduced); English → stone **37.3% exact**, **27.1% declined with a reason**, **35.6% wrong without saying so**. **No bar has ever been set for this goal** — D50 set one for goal 1 only.

**Why goal 1 is nearly done and goal 2 sits at a third, and it is not effort.** An LLM reads context and guesses past an undetermined sentence plausibly; a script cannot. Every place the corpus leaves a split undecided is a hard stop for goal 2 and survivable for goal 1. **Work that makes the corpus more determined serves goal 2. Work that makes it larger or more expressive serves neither.**

**Instruments, tagged.** *Goal 1*: the acceptance test (D3, D50, D75, D93), the composition audit as its pre-run step (D113), the drift test (D15, D74). *Goal 2*: `realize.py --score`, `compose.py --score`, the G34 undetermined-split report. *Both*: the regression test of the checks (D101) and mutation testing (D104) — they keep the checks honest and so protect every number above. *Neither*: the coverage test (D121) and the dictionary probe (D122), which measure expressivity. That is D1's goal of two people holding the language in their heads, and **an LLM can translate a language that cannot say Monday, and so can a script.** Both stay as instruments; neither drives work.

**Stood down under these objectives, recorded rather than abandoned (D125).** The **recoining of the 17 roots from D123–D124**: the defect is real and measured — they sit in minimal-pair neighbourhoods averaging 11.88 against the lexicon's 5.79, because all 17 went into one second-syllable row, taking it from 21% to 30% and pushing *want*, *use* and *week* up six neighbours each — but a script reads exact tokens and so does an LLM, and neither mishears a vowel. It is worth nothing to either goal, and it gets harder every session, so it is the first thing to fix if D1 becomes current again. Also stood down: the **social register** (sorry, thank, welcome, bye), the remaining D122 expressivity gaps, and any coining not required to determine a sentence.

**The next two pieces of work that serve an objective.** *Goal 2*: **categorise the 507 silently-wrong compositions** — the largest unexamined thing in the repo and the whole of goal 2's gap. How many are the 92 G34 splits, how many a rule the corpus never stated, how many ordinary parser shortfall. *Goal 1*: fresh held draw, composition audit, then spend a run.

## The language
- Purpose: asking, code, discussion, engineering and science talk between two people; the corpus is its codex. (D1, D34)
- Script: 74 syllable glyphs as Hangul blocks that spell their own sound (initial + vowel, no final), `spec/codepoints.json` from `scripts/compose_hangul.py`; sounds in `spec/phonology.md`. 1–3 tokens per glyph. Priors accepted in D52 and now tested four times: acceptance tests 3, 4 and 5 and the first drift test all found no Korean reading and no slide toward Korean. (D28, D37, D52, D86, D98)
- No Latin form of any word exists anywhere. IPA describes sounds only. (CLAUDE.md rule 3)
- **Word order is closed and enforced at both levels: subject, n-times, object, adverbials, verb.** The subject comes first, always (131 to 13 in the corpus, the 13 corrected under D102 and D103). An n-times phrase precedes the object, because counting material precedes what it counts (9 to 0; D48, D97). Every other adverbial follows the object and stands immediately before the predicate — time, place, while, until/since, because, before/after, for, from, and the with-phrase of manner, instrument or comitative. Every type is unanimous after D103. The with word coordinating two arguments into a subject is part of the subject, so it precedes the object (15 lines, all pronoun + pronoun + with). `validate.py` **errors** on a violation of either rule. (D78, D87, D97, D98, D102, D103)
- One open root class; roots are neither noun nor verb. Verb template: root, negation, tense, evidential, stance, subordinator; only the evidential is mandatory. Arguments bare, or root plus one case marker (object, location, possessor). **A marker follows the word it marks: no case marker, relational word or question particle opens a sentence or follows another marker, and or/but stand between what they join (D59, D104).** A pronoun, the interrogative, a numeral or a root before a root is a determiner or compound; the personal pronouns are not determiners, because possession takes the possessor case. No adjectives, no adverbs. (D26, D29–D33, D44, D48, D64, D102, D104)
- Evidentials, obligatory: direct, reported, inferred, general. Stance, optional: intend, predict, propose, trust, risk, assert. Tense: past, present unmarked, future (temporal only). Irrealis clauses (conditions, modal complements) take the general evidential — checked over the whole corpus at 0 violations (D101). A first-person intend predicate carries the future marker; the predict stance takes the inferred evidential. (D30, D31, D34, D60, D61, D88)
- Four modal roots share a syllable and one construction over a subordinate clause: able, want, should, allow. (D61, D63)
- **A gap is decomposable or it is primitive, and the two get different treatment** (D123, D124). Decomposable means existing machinery reaches it without lying — half, Monday, morning — and costs no roots. Primitive means nothing you can assemble lands on it: *similar* is not not-same, because not-same is **different**, the opposite rather than the middle. Check the lexicon one root at a time before calling anything primitive.
- **Typical is what usually is; correct is what should be** (D124). Two roots, and science talk needs both — having only the second is why the corpus could call a result wrong and never anomalous. **Anomalous is typical negated and gets no root**, because not-typical leaves no third case, unlike not-often against rare (D62).
- **Three systems that cost no roots** (D123): fractions are numeral + possessor + the part root (the possessor does the denominator's work); weekdays are the week-day compound plus an ordinal, **day one is Monday**; parts of day are early-day for morning and late-day for afternoon. Evening is not distinguished and the collapse is accepted. Reach for a construction before a root — D122 measured 66 words solved by construction against 2 by compound.
- **Approximation is a determiner, in the quantifier slot and on the quantifier consonant** (D123). Before a numeral, "about five"; before a stative root, "almost done". **It hedges an amount or a degree, never a claim** — hedging a claim is the stance slot, risk and propose, and running the two together would undo D34.
- **Six colours, a family on the look syllable** because a colour is a seen quality (D123). Black, white, red, green, blue, yellow, by vowel. Six and no more, in the order languages acquire basic colour terms, so a seventh has a determined place. No light, dark or saturation terms.
- Semantic carving, `lexicon/README.md`: knowing ×3, error ×3, asking ×3, change ×3; experiment/test/try = 1. Saying is one root; the message root is a noun. A person choosing takes the choose root; the condition/branch root is the branch point in code. Quantified duration takes the measure root, unquantified takes the time root. (D35, D90, D92, D95)

## Tools
- `scripts/translate.py`: gloss, check, lookup, examples need no key; to-english, to-stone, roundtrip need `OPENROUTER_API_KEY`. `scripts/stonelib.py` holds the parsing both it and the validator use. All three layers have been run against the live model. (D72, D76)
- `scripts/drift_test.py`: the drift test, both arms. Needs the key. (D74)
- `scripts/correct_corpus.py`: the reviewed route for correcting an existing corpus line; every correction needs a `why`, dry run by default. (D81)
- **A model-free translator, both directions** (D106, D107), wired into `translate.py` as `english` and `stone` (no key; `to-english`/`to-stone` remain the model-backed pair). `scripts/realize.py` is stone → English at 22.2% exact and 27 of 30 meaning-correct by hand; `scripts/compose.py` is English → stone at 40.4% exact (35.5% held), and it **declines with a reason** on another 24% instead of guessing. Missing English is treated as a phrase-building problem, not a dead end (D108): a word may be derived from a root (`slowly` → quality + the with word), or be a construction (`everyone`, `while`, `because`, `but`) — but one of the three carves the English cannot decide (`hold`, `choose`, `variable`) still declines, which is where this language and toki pona part company. `compose.py --roundtrip` composes and realises back, reporting drift as a confidence (0.69 mean where right, 0.44 where wrong — a signal, not a verdict), which is the seam a model would plug into. Three of the four steps are near-free because the corpus already forced them: fixed cues (D84–D105), a near-functional lexicon, and word order closed (D102, D103). The fourth, English parsing, is ordinary NLP. **Neither is a substitute for the acceptance test** — both were written from the spec, so they say nothing about whether a cold reader could reconstruct the language.
- `realize.py --ambiguous`: the 92 sentences (8 held) where `[root][root][marker]` is structurally undecidable (G34). **Re-run it rather than quoting this number** — it read 64 for three sessions after the corpus it described had grown by 400 sentences.
- `scripts/regression_test.py`: re-runs every check in `validate.py` against the pre-correction form of all 62 corrected lines, from `tests/fixtures/pre_correction.jsonl` — 28 fronting, 17 object-order, 6 rule, 2 leak, 1 collision, and 11 corrections no check can see. Run it beside `validate.py`. (D101–D103)
- The guardrail is a template check, not a meaning check: it catches a broken verb ending, a non-word, and since D104 a marker with nothing to mark; it does **not** catch a wrong case marker. `check`'s modal advisory is narrower than it sounds — it fires when the complement is a bare root, and says nothing when a complement keeps the general evidential but drops the subordinator, or carries the wrong evidential (D76, D101). Read the parser-produced gloss, not the verdict.
- See `scripts/README.md` for what each script does.

## The method
- `corpus/` is the deliverable; spec and lexicon are scaffolding. Done = a cold session with only the train corpus translates held-out sentences both ways. (D2, D3)
- Acceptance test: the two directions go to **different** agents, so no agent sees both sides of a held sentence (D75). And a held sentence may not be a copy of a train sentence, which is the same leak one level up — the validator now fails on it (D93). Check the held set before the run, not only through the validator.
- **A run's number describes the corpus it was run on.** Changing the *train* split changes the measurement even when no held line moves, because what the test measures is reconstruction from the train split. D102 and D103 both said test 5 was "unaffected" because they touched no held line; that was the wrong test, and 43 of the 888 pairs those agents read have since changed or been added (D104).
- Drift test: two arms, the long session and the fresh-session control. A run without the control is not interpretable. (D74)
- **The coverage test answers what the acceptance test structurally cannot: whether the language reaches past its own corpus** (D121). Every acceptance-test sentence was drawn from the corpus and every corpus sentence was written with the lexicon open, so a thing the language cannot say was never written down and is never scored. The coverage test inverts the deprivation: a cold agent with only a domain description writes the English, two cold agents with the **full** materials translate it, and each line is classed OK / CLUMSY / BLOCKED_WORD / BLOCKED_GRAMMAR. Two translators because tolerance for paraphrase is the measurement. No bar, and none should be set. A fresh blind draw every run — reusing a source set after coining against it measures the coining. Protocol in `tests/README.md`.
- **The dictionary probe measures how ordinary words get solved, not whether sentences can be said** (D122). A fixed frequency list (OpenSubtitles, the source `data/ko_frequency.json` already uses), an explicit stop list, mechanical lemmatization, then two cold arms per band classifying each word ROOT / DERIVED / CONSTRUCTION / COMPOUND / GAP / INFLECTION with a scope call on every gap. It has no author bias about the domain, which the coverage test does. **Nothing it finds goes into the corpus** — it decides what gets looked at, never what gets added. Re-run after any coining round.
- Words enter only via `scripts/validate.py --add`; run `scripts/validate.py` and `scripts/regression_test.py` before every commit. Multi-syllable forms are rejected if they are Korean words (`data/ko_frequency.json`) or if they read as a shorter root plus a verb ending. (rules 4, 6; D55)
- Batches: one subagent per domain with a disjoint first-syllable pool; `scripts/merge_batch.py BATCH_DIR` dry-runs, the maintainer reviews, `--apply` writes. It only appends. (D58, D66)
- **The full treatment for a word-order gap, done three times and it works:** count every sub-type rather than the ones the gap was logged under, choose the rule the counts already support and say why, correct every violating line through `correct_corpus.py` with the attested parallel named in each `why`, write the check, show it firing on the pre-correction forms and silent on the corrected corpus, put those forms in the fixture — and then mutation-test it. (D78, D102, D103, D104)
- **Two ways to test a check, and they answer different questions.** `regression_test.py` proves it catches the defects that *were* in the corpus. **Mutation testing** — take the held sentences that exercise a rule, mutate each into a violation, require the check to fire — proves it catches defects nobody happened to write. Do both when a check is added or widened, and record the score; `tests/README.md` has the protocol and how to read a non-catch. (D82, D101, D104)
- **An exemption only the check knows about is the same defect as a convention only the prose knows about.** The cue check ran as "any marked slot carries a cue" where the rule says "every", which was right for 24 lines because D88 pins the predict stance to the inferred evidential — an exemption in force everywhere and written nowhere — and wrong for the one line that hid behind it (D105).
- **A check earns its count by what it enumerates, what scope it walks, and what it has been made to miss.** Three sessions running the gap was in the check, not the rule: shapes it never asked about (D102), clauses it never walked (D103), a marker it was told to ignore for a reason that had expired (D104). The gloss-collision report is the standing example — it cannot see the say/message carve it was built for, nor the D95 choose carve, because both live in `sense_notes` prose while it reads the `gloss` field.
- `validate.py` **errors** on: an adverbial before the main-clause subject (D78, D102); an adverbial before the object rather than after it (D103); a marker with nothing to mark (D104); a held sentence whose script or English is verbatim in train (D93); a first-person intend predicate without the future marker; the predict stance on the direct evidential; **every** marked evidential or stance whose English carries none of its own cues, with three exemptions — the imperative is the intend cue, an irrealis protasis needs no general cue, and the predict stance supplies the inferred evidential (D84, D88, D91, D94, D105); and `corpus/README.md`'s cue sentence or feature-tag list drifting from the code and the corpus (D101). It **reports**: English words naming more than one predicate root, baseline 1 — "hold", where transitivity separates the two (D94).
- **The acceptance test is not a linter.** Test 4 cost ~683k subagent tokens and eleven of its twelve misses were findable by static audit; that is what the checks above are for. Spend a run on the question only it can answer. (D94)
- Four places a defect hides, all four found by hand rather than by a miss: a convention that lives only in prose (D82, D87); **a carve the lexicon states and the corpus contradicts** (D95); **a rule the spec states and the corpus contradicts** (D99); and **a rule the spec states, the corpus mostly follows, and a check appears to enforce** (D101–D104). Read the corpus counts, not the prose, and read what the check actually looks at.
- Codepoints: BMP PUA is stripped by Claude's input pipeline (D20); Yi Syllables trip a content classifier on the chat surface (D52). Neither is usable.

## Open
- **G34, uncounted: 92 sentences (8 held) where `[root][root][marker]` is structurally undecidable** — a compound, or a bare argument plus a marked phrase. Found by `realize.py --ambiguous` (D106), which breaks the tie with the subject rule and is right everywhere it was checked by hand. Not yet triaged into genuinely two-way against merely structurally so; a held sentence the script does not determine is what cost acceptance tests 3 and 4.
- **Two fresh-set runs now agree at about 90%** (test 6: 91.2%, test 7: 89.6%), against test 5's 97.5% on a set three runs had combed. **Neither found a single real model miss.** The gap is entirely corpus work.
- **Word order is closed at every level** (D120 closed the last of it): subject, n-times, object, then adverbials in their own order — with-phrase, time/place/region/before-after, recipient — then the verb. The recipient is last because D57 made it an argument rather than a circumstance.
- **New sentences are the most dangerous content in the corpus** (D119): they are written against the rules no check covers yet, because every check here exists only after something broke. Two sentences written 20 minutes before test 7 contradicted a 19-line pattern and cost the run its whole margin. Read the existing examples of a construction before writing another one.
- **G35 is closed** (D114–D117): a region nominal compounds with its landmark and takes no possessor; locating a definite thing takes the place root and existence takes the hold root; a possessor predicate takes no leading pronoun; a subordinate clause states its own subject. Two of its six are **not** reopened — s1057 is the standing true/hold ambiguity and s0202 the standing "hold" gloss collision.
- **The composition audit is a pre-run step now** (D113): draw a held set, run `validate.py`'s composition report, teach the pairs it names in train, *then* spend a run. A count per feature says nothing about compositions — `agentless` 17 and `modal` 56 had never once met, and that pair failed test 6.
- **The next run needs a fresh draw.** This held set chose train sentences (D113) and ten of its lines were corrected, so it is no longer clean; the eligibility rule blocks corrected lines automatically.
- **No word-order gap is open.** G31, G32 and G33 are closed by D102 and D103; G1–G33 are all closed. New gaps get logged as G34+.
- Still undecided below the object: **where two adverbials sit relative to each other.** Exactly one line has the configuration (s0625) and it is placed to keep both attested adjacencies rather than to settle it. (D103)
- D11 coverage: every word appears in ≥3 train sentences (met first at D85, still true), and no held sentence rests on a thin or absent word (D101). 31 features are still under 10; held s0947 rests on the thinnest, `not-yet` at 2 train sentences.
- Three language ambiguities left standing, none closed because closing any of them would change the language for one sentence's sake. The true/hold root does existence and truth both, so an argument plus that root negated reads as both "there is no X" and "X is not true" (D76). A bare-noun subject followed by a numeral plus the object marker is indistinguishable from a root-plus-numeral label carrying the object marker, so s0277 parses two ways (D98). **And the comitative with-phrase against the coordinated subject: position relative to the object is the only thing separating them, so moving one across the object does not mark the sentence, it changes it** — sliding s0022's comitative before the object yields train line s0515, "We tested it". D98 recorded this as holding only in a clause with no object; it is wider (D104).
- A fourth, general rather than per-sentence: a root standing before a root is a compound and a demonstrative before a root is a determiner, so `우 와 후` reads as both "it, after the test" and "after that test". The corpus relies on the English to choose, in s0109, s0221, s0623 and six others. Not closed; recorded because it is why D102's counts are conservative.
- The thin edge of G30: D96 keys the anaphoric-"one" rule off whether the English names the head in the *same* sentence, and says nothing about a head named in a previous one.
- w0100's `sense_notes` still reads "Verbal: to choose" after D95 moved a person choosing to the choose root. Reported, not edited: `lexicon.json` is not edited by hand (D101).
- **Compounding is not available to this language, and D35 is the cause** (D122). Two cold readers told that compounding was encouraged used it 6 times in 800 words, while using constructions 66 times — the same build-from-parts instinct, applied where it works. Toki pona's `ilo nanpa` runs on `ilo` being vague; three roots for knowing and three for error is the opposite bet, and **sharp roots do not stack** — machine + small is not an underspecified screwdriver, it is false. D107 said the build-it half of toki pona was already policy and unimplemented; the missing half is that it cannot be implemented on a vocabulary carved this finely. Doing it would mean coining deliberately generic roots to serve as compound heads, against D35. Open, not decided.
- **Two registers are absent and both are inside D34** (D122): the social (sorry, thank, welcome, bye, appreciate) and the approximate (enough, almost, half, quite, rather, about). The language has four obligatory evidentials and six stances for epistemic precision and **nothing at all for approximation**, which for science talk is the sharper of the two.
- **A third gap category: a rule stated and never taught** (D122). *hundred* is ten-before-ten by grammar line 47, s0103 shows `two-ten-five-one` for twenty-six, and both cold arms still called it a gap because no corpus line demonstrates it. Invisible to the acceptance test, which only asks about sentences the corpus contains. Needs a sentence, not a root.
- **The generic compound-head roots were proposed and rejected** (D123). Implement, path, surface, container, substance, on the `ilo` model. They collide — bracket and clamp both become implement-plus-hold — so they buy reach by spending the distinction D35 bought the language's originality with. And they do not help: not one of the in-scope gaps (similar, meet, lose, free, team, tired, normal, eat) is compoundable, because they are abstract primitives with nothing to intersect. The proposal fixes the physical-equipment band, which D34 never claimed. Coin the primitives directly instead.
- **The coining round D122 opened is closed** (D123, D124). Its in-scope list is spent: the decomposable half built from existing machinery, eleven primitives coined, and three concepts ruled out by carve rather than coined — *food* (a root is neither noun nor verb, so the eat root already is it), *anomalous* (typical negated), *the telephone* (the act is say/tell; the device is the physical-object class D34 never claimed).
- **Nothing since the last acceptance run has been measured.** 129 sentences and 18 roots have landed, so every prior number describes a corpus that no longer exists (D104). **Re-run the dictionary probe now** — the list is fixed so the number is comparable, and moving it was the point; the baseline is 67 / 44 / 37 / 27 by frequency band and 65% of running text by token (D122).
- Still absent, still undecided: the **social register** (sorry, thank, welcome, bye), which is inside D34 and was deprioritised by the maintainer rather than closed; any **lateral axis**; colour beyond the six basic terms.
- **Physical-object vocabulary is absent by design, not by oversight** (D1, D34): ~170 content roots and close to no nameable objects. Whether that should change is a purpose question nobody has asked. Also absent and not physical: the version-control sense of branch (the D35 carve is the branch point in code, and both cold arms correctly refused to stretch it), any lateral axis (up, down, inside, outside, near, far — no left or right), and fractions.
- **"The lexicon is not the bottleneck" holds only over a closed set** (D121). It was measured on the corpus's own English — 206 entries covering 1295 sentences, all 31 thin features closed with no new root — and on novel English in the same register the lexicon is the only bottleneck there is. **Counting a vocabulary over sentences written by people holding that vocabulary measures nothing about reach**, which is D112's error in a different instrument.
- **A gloss-string lookup is not a coverage measure** (D121). `compose.py`'s reverse index matches glosses, so a first pass called error, fresh, row, blank, heat and broken missing when all six have roots. Caught by reading the list rather than the total.
- O5 teaching materials (after the corpus). O7 formal code register (later). Derivation suffixes: when roots need them.

## Tests
- Test 1 (Yi glyphs): 13/13 comprehension, 12/13 exact production. Test 2 (Hangul): 21/21 and 20/21. Both were run before D75, so both had the direction leak: discount them.
- Test 3 (2026-09-15, all 107 held, directions split): stone → English 107/107 meaning-correct; English → stone 87/107 = 81.3% exact — **failed** the ≥90% bar. 11 of 20 misses were the corpus declining to decide or contradicting itself; 4 were real model misses; 3 changed meaning. `tests/results/2026-09-15.md`.
- Test 4 (2026-09-15, all 121 held, 8 agents, directions split): stone → English 121/121 meaning-correct; English → stone 109/121 = 90.1% as scored, 107/119 = 89.9% discounting two leaked items (D93) — **did not meet the bar**. `tests/results/2026-09-15-b.md`.
- **Test 5 (2026-09-15, all 121 held, 8 agents, directions split): stone → English 121/121 meaning-correct; English → stone 118/121 = 97.5% exact — PASSED, on the corpus as it stood at 31b0c11.** Per chunk 30/31, 30/30, 28/30, 30/30. On test 3's 107 sentences, 104/107 = 97.2%; on the 85 no test has ever touched, 83/85 = 97.6% (D100). Three misses: one real model miss (s0022), one construction with no train example (s0522, fixed by D97), one rule the corpus followed without stating (s0529, fixed by D96). `tests/results/2026-09-15-c.md`.
  **Standing of that number today (D104):** the held set is byte-identical, no held sentence violates the D102 or D103 rules, and the 12 held sentences that carry both an object and an adverbial all sat on the majority side, whose train support went from split to unanimous. But 33 train pairs changed and 10 were added, so the run measured a corpus that no longer exists. Margin is 10 items — 118 against the 109 the bar needs. Very probably still passing; not measured.
- **Drift test 1 (2026-09-15): no drift on all three signals.** Probe divergence 0/12; filler accuracy 28/40 in the long session against 24/40 in the fresh-session control, with the long session right on all 4 disagreements. Zero Korean leakage in 461 tokens. `tests/results/2026-09-15-drift.md`.
- **Mutation tests of the checks (D104):** the D103 object rule catches 6 of 8 held sentences mutated into violations, the two non-catches being the comitative/coordination collision above rather than misses; the D102 subject rule catches 16 of 16, after the mutation test found that a fronted with-phrase was invisible to it.
- **Dictionary probe 1 (2026-09-15, 800 words, 8 cold agents, two arms per band): coverage 67% / 44% / 37% / 27% across frequency ranks 1–200, 201–400, 401–600, 601–800.** Agreement 91–95% exact. Of 348 words both arms solved: 252 root, 66 construction, 28 derived, **2 compound**. 76 in-scope gaps. `tests/results/2026-09-15-g.md`. (D122)
- **Coverage test 1 (2026-09-15, 60 blind sentences, two full-materials translators): 39 and 38 sentences produced, all 77 well formed, and zero BLOCKED_GRAMMAR from either arm.** Agreement 53/60 on sayable against blocked, every disagreement on the CLUMSY/BLOCKED line rather than OK against BLOCKED. Sayable by band out of 12: discussion 12 and 11, code 11 and 10, experiments 9 and 8, scheduling 6 and 7, **physical equipment 1 and 2**. `tests/results/2026-09-15-f.md`. (D121)
- Bar: 100% / ≥90% with every miss grammatical. (D50) **Met at test 5.**

## Lexicon — 224 entries, target 300 (D54)
- w0001–w0025 grammatical pieces, one syllable, slot = consonant and value = vowel; pronouns are the bare vowels. (D39)
- w0026–w0038 carve roots, each carve a consonant family. (D42)
- w0039–w0058 inquiry core; one interrogative root does every question word. (D45)
- w0059–w0077 statives, numerals 1–10 quinary, before/after; deixis from pronoun + location/time. (D48)
- w0078–w0130 first subagent batch: code, experiments, discussion. (D58)
- w0131–w0139 or, but, if, the four modals, every/all, no/none. (D59–D63)
- w0140–w0196 second subagent batch: physical handling, time and process, work. (D66)
- w0197–w0206 calendar and clock units (week, month, year, hour, minute) plus use, need, example, word, size. (D85)
- w0207 the approximation determiner (a reserve syllable spent by decision), w0208–w0213 the six colours on the look syllable. (D123)
- w0214–w0224 the eleven primitives: six in the person family (meet, team, eat, drink, sleep, tired), then typical, similar, free, sudden, lose, each joining the family it carves against. (D124)
- Syllable budget: 72 of 74 single syllables used, 2 in reserve. **A single syllable is spent when the root belongs to an existing single-syllable family, never on frequency alone** — the approximation determiner sat with every/all and no/none, while *similar* belongs beside *same* and there is no free syllable in that row, so it took two (D123's note on this is corrected by D124). The two left are held for a root belonging to the modal or the time-unit family, which is what those syllables already prefix. New roots are two syllables unless a reserve syllable is spent by decision.
- Coverage test 1 measured the shape of what is here: discussion is complete, code and experiment talk close to it, the calendar half-built, physical objects absent. (D121)

## Corpus — 1424 sentences, 125 held
`corpus/corpus.jsonl`; writing conventions and the English cue phrases in `corpus/README.md`. Constructions decided so far: imperative = you + intend stance; existence and having = the hold root, no have and no exist; equative = second term takes the verb ending, no copula; recipient = location case, after the object; and = juxtaposition or the with word, with or and but as words; if follows its clause; quantifiers every/none are determiners and bare arguments are number-neutral; no passive, drop the subject and keep the object marker; comparison uses the from word; manner = quality + the with word; because = clause + the from word; therefore = bare juxtaposition; become = stative clause + begin; ordinals = numeral after the root; still = the continue root; until/since = clause + time + before/after; an alternative question coordinates the smallest constituent that differs; a modified anaphoric "one" repeats its head root when the English names that head in the same sentence and takes the thing root otherwise. (D43–D49, D51, D56–D70, D89, D96, D103)
- s0866–s0889 added 2026-09-15 for the constructions test 3 found too thin. All pass in tests 4 and 5. (D77)
- s0890–s0992 added 2026-09-15 with the ten calendar, clock and utility roots. (D85)
- s0993–s1009 added 2026-09-15 for the three constructions test 4 found too thin. All pass in test 5. (D92)
- s1010–s1019 added 2026-09-15 after test 5: the interrogative n-times phrase with an object (D97), the anaphoric-"one" rule (D96), and the condition/branch root's coverage floor after D95.
- s1368–s1424 added 2026-09-18 with the eleven primitives (D124), all train, no new feature tags. The read-through after writing caught two defects no check can see: a script saying *not typical* under an English saying *sudden*, and a reported evidential with no English cue. **A new sentence is read by a person or it is not read at all** (D119).
- s1296–s1367 added 2026-09-15, the first coining round driven by measurement (D122 → D123): 24 colour, 13 approximation, 12 fraction, 12 weekday, 12 part-of-day, all train. Held ratio is now 9.1%; the next round draws fresh anyway.
- `spec/grammar.md`'s word-order section corrected under D99 and D101, rewritten under D102 and D103; it is now closed.
- **Sixty-three lines corrected through `correct_corpus.py`**: 29 under D87–D93, 3 under D94, 2 under D95, 11 under D102 (G32), 17 under D103 (G31, G33 and two second-clause G32 stragglers) and 1 under D105 (s0010's English, which carried only one of its two cues). The pre-correction form of every one of the 63 is in `tests/fixtures/pre_correction.jsonl` with the check that catches it — 28 fronting, 17 object-order, 6 rule, 2 leak, 1 collision, and **11 that no check can see**.

## Editing this file
Rewrite it whole. Patching it by string replacement has silently failed before (D73).
