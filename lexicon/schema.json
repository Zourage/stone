{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "stone lexicon entry",
  "type": "object",
  "required": [
    "id",
    "form",
    "pos",
    "gloss",
    "added"
  ],
  "properties": {
    "id": {
      "type": "string",
      "description": "stable id, e.g. w0001"
    },
    "form": {
      "type": "string",
      "description": "the word, in script: only the 74 codepoints in spec/codepoints.json, 1-3 syllables"
    },
    "pos": {
      "type": "string",
      "enum": [
        "root",
        "func",
        "affix",
        "num"
      ],
      "description": "root = open class, neither noun nor verb (D29); func = closed class (case, question particle, pronouns, relational words, D33); affix = piece of the verb ending (D32, D33); num = numeral (D36)"
    },
    "gloss": {
      "type": "string",
      "description": "English meaning, with its boundaries stated"
    },
    "sense_notes": {
      "type": "string",
      "description": "what this word covers that English words don't, and vice versa; which side of a semantic carve it sits on (D12, D35)"
    },
    "root": {
      "type": "string",
      "description": "id of the root if derived"
    },
    "derivation": {
      "type": "string",
      "description": "which suffix/rule produced it"
    },
    "examples": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "corpus sentence ids where it appears"
    },
    "added": {
      "type": "string",
      "format": "date"
    }
  }
}
